const nedb = require('nedb');
const express = require("express");
const path = require("path");
const db = new Datastore({ filename: '../goals.db', autoload: true });



const app = express();
class healthApp {

}